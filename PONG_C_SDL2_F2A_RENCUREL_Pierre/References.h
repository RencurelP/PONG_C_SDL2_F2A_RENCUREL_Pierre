#ifndef REFERENCES_H_INCLUDED
#define REFERENCES_H_INCLUDED

//Compilation des references aux bibliothèques et constantes

#include <stdio.h>  //Inclusion des inputs et outputs standard
#include <stdlib.h> //Inclusion des librairies standards
#include <SDL2/SDL.h>   //Inclusion de la SDL2
#include <SDL2/SDL_ttf.h> //Inclusion de l'extension TTF
#include <math.h>   //Inclusion de la bibliothèque Math pour effectuer les calculs du tracé de la balle

#include <time.h>   //Inclusion de la bilbiothèque time pour calculer et limiter le framerate

#define SCREEN_WIDTH 600    //Definition par une constante de la largeur maximum de l'écran
#define SCREEN_HEIGHT 500   //Définition par une constante de la hauteur maximum de l'écran
#define RAYON_BALLE 20      //Définition par une constante du rayon de la balle

#endif
